"""Flow Studio — stateful orchestration layer over Flow Kit's stateless proxy.

Adds a SQLite-backed project/scene/shot model + a media cache, powering the
Flow Studio web app. See video-app.md for the full design.
"""
